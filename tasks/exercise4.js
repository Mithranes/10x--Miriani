let attackPower = 100;
let armor = `Eternal Plate`;
let shield = true;

console.log(`Sir Debugalot is ready for battle
    He wields a sword with ${attackPower} attack power,
    wears the mighty armor given by god ${armor},
    and ${shield ? `carries a shield used against Medusa` : `no shield relying on his armor`}.`);
